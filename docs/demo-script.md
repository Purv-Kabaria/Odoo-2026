# AssetFlow — 5-Minute Demo Script

A judge-facing walkthrough for a screen recording. Total runtime target: **5:00**. Don't linger on any single screen — the goal is breadth (every feature touched, every edge case shown once) over depth. Log in fresh for each role switch called out below; demo data is seeded via `pnpm db:reset:populate` (see README for the full credential table).

| Time | Screen | Show | Say |
| --- | --- | --- | --- |
| 0:00–0:15 | — | Nothing on screen yet, or the landing page | "AssetFlow is an enterprise asset-management app — register assets, allocate them individually or in bulk kits, book shared resources, run maintenance, audit inventory, and report on all of it, behind real RBAC." |
| 0:15–0:40 | `/login` → `/assets` | Log in as `admin@assetflow.demo` / `Password123!`. Point out the role badge / nav changes. | "Four roles: Admin, Asset Manager, Department Head, Employee — every API route re-checks the role server-side, not just the UI." |
| 0:40–1:10 | Asset Directory (`/assets`) | Search by tag/name, filter by status, then click the QR scanner icon and scan an asset's code (or hold a printed/on-screen code up to the webcam) — the search bar fills and filters instantly. Open one asset's detail page showing its allocation + maintenance history inline. | "Assets get an auto-generated tag, category-specific custom fields, and full history in one place. You can type to search, or scan a QR code straight into the search bar — same debounced filter either way." |
| 1:10–1:55 | Allocation & Transfer (`/allocations`) | Allocate an available asset to an employee. **Edge case:** try to allocate an already-held asset — show the red conflict banner naming the current holder, then submit a transfer request instead of a raw error. Switch to the "Kit" tab, pick a multi-asset kit, and allocate the whole thing to a department in one action. | "Double-allocation isn't just blocked in the UI — there's a partial-unique index in Postgres backing it, so it can't happen even under a race. Kit allocation validates every asset in the kit up front — if even one is already held, the whole batch is blocked and that asset is named, not a partial allocation." |
| 1:55–2:35 | Resource Booking (`/bookings`) | Book a bookable resource on the day timeline. **Edge case:** attempt an overlapping slot — show the live conflict preview before submit, then book back-to-back (should succeed — half-open interval). Point out the check-in deadline shown on the confirmed booking card. | "Overlap is enforced by a GiST exclusion constraint at the database level, not an app-side check. Every booking also carries a 15-minute check-in deadline — a background sweep nudges you, gives one 5-minute grace period, then auto-cancels and frees the resource if you never check in." |
| 2:35–3:20 | Maintenance Kanban (`/maintenance`) | Raise a request (mention the mic button — dictate the issue description live, or note it works if the browser prompts for mic access). Drag a card Pending → Approved. Approve → assign a technician → progress → resolve. Point out the AI-flagged card and click "Verify & Retire." | "Resolving fires a non-blocking LLM call that looks at acquisition cost and maintenance history and flags assets not worth repairing. A manager reviews the reasoning and retires with one click — the asset is globally retired, not just this one ticket." |
| 3:20–3:45 | Maintenance again, or Allocation return flow | Show the Condition Notes field with voice dictation on the asset-return modal. | "Voice input is a reusable component — same hook and button power both this and the maintenance description, and it degrades gracefully with a toast if the browser doesn't support the Web Speech API or the mic is denied." |
| 3:45–4:10 | Audit Cycles (`/audit`) | Open the one seeded closed cycle — show the discrepancy report (a DAMAGED item that auto-raised a maintenance request). | "Audits verify assets against their expected holder or location; anything missing or damaged gets flagged, and a damaged item automatically raises a maintenance ticket — no manual handoff." |
| 4:10–4:40 | Reports (`/reports`) | Show the KPI tiles, utilization-by-department chart, and the compact-by-default tables — click the chevron to expand one. Export a CSV. | "Tables default to compact, Vercel-dashboard style — click the arrow to expand. Every report is Redis-cached for 60 seconds and exports to CSV regardless of the collapsed/expanded state." |
| 4:40–5:00 | Notifications bell (any screen) | Trigger an action that fires a notification (e.g. approve something as a different role in another tab, or just open the bell) and show it arriving live. | "Notifications push live over server-sent events, backed by Redis pub/sub and a durable Postgres row so nothing's lost — plus a set of background sweeps that generate their own alerts, like the booking check-in reminder you just saw. That's the whole app — thanks for watching." |

## Edge cases worth calling out verbally if time allows (skip if tight)

- **Pending approval**: a self-signed-up user can't log in until an Admin approves them — the login error message says so explicitly, not a generic "invalid credentials."
- **Kit allocation conflict**: same as the single-asset conflict banner, but for a whole kit at once — attempting to allocate a kit with even one already-held asset blocks the entire batch and names the conflicting asset, rather than partially allocating.
- **Illegal Kanban drag**: dragging a card backwards or skipping a stage is rejected client-side with a toast and the card snaps back — the drag only ever calls a legal transition endpoint.
- **Booking overlap**: shown above — this is the single most impressive "the database, not just the app, prevents this" moment, worth the extra 5 seconds if you have them.
- **Booking auto-cancel**: if you have a spare minute before recording, create a booking with a very short window in a seed script or via the API so its 15-minute check-in deadline is already close, then show the reminder notification and (if you can wait it out) the auto-cancel — otherwise just narrate the mechanism from the booking card's visible deadline.
- **Unsupported browser for voice input**: if recording in a browser without `SpeechRecognition` support, the mic button is visibly disabled with a tooltip rather than silently doing nothing.
- **QR scan denied/no camera**: the scanner modal shows a clear permission-denied or no-camera message rather than a blank or frozen video element.

## Recording tips

- Use a light-mode and dark-mode pass if you have 2 takes in you — the toggle is in the navbar — but pick ONE for the actual 5-minute submission to stay on time.
- Keep the browser at a standard laptop width (~1440px) for the main walkthrough; a 10-second mobile-width cutaway on the Maintenance Kanban (resize to ~390px) is a good way to prove the responsive claim without a second full pass.
- If you don't have a physical QR code to scan, generate one from any asset's `qrCode` value ahead of time (any online QR generator) and display it on a second screen or phone to scan from — much smoother live than fumbling for one during the take.
- Narrate over actions rather than pausing dead air — the table above is paced assuming continuous talking.
